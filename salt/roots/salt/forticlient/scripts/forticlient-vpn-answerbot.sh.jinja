#!/usr/bin/expect -f

## Disable timeout
set timeout -1

spawn /usr/local/bin/start-vpn

expect "certificate password:"
send -- "{{ salt['pillar.get']('credential:certpasswd') }}\r"

expect "Confirm \*:"
send -- "y\r"

expect eof
